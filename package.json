{
  "name": "taxi-fare-app",
  "version": "1.0.0",
  "private": true,
  "description": "Professional taxi driver income & expense tracking app for South Africa",
  "scripts": {
    "cap:add:android": "capacitor add android",
    "cap:sync": "capacitor sync android",
    "apk:debug": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/cli": "^7.4.3"
  }
}
